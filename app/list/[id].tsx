import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Post = () => {
  return (
    <SafeAreaView>
      <Text>Post</Text>
    </SafeAreaView>
  );
};

export default Post;
